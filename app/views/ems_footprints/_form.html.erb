<%= form_for(@ems_footprint) do |f| %>
  <% if @ems_footprint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ems_footprint.errors.count, "error") %> prohibited this ems_footprint from being saved:</h2>

      <ul>
      <% @ems_footprint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
