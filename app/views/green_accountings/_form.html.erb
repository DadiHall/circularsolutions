<%= form_for(@green_accounting) do |f| %>
  <% if @green_accounting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@green_accounting.errors.count, "error") %> prohibited this green_accounting from being saved:</h2>

      <ul>
      <% @green_accounting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
