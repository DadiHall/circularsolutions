<%= form_for(@energy_analysis) do |f| %>
  <% if @energy_analysis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@energy_analysis.errors.count, "error") %> prohibited this energy_analysis from being saved:</h2>

      <ul>
      <% @energy_analysis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
