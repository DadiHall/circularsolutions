<%= form_for(@lca) do |f| %>
  <% if @lca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lca.errors.count, "error") %> prohibited this lca from being saved:</h2>

      <ul>
      <% @lca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
